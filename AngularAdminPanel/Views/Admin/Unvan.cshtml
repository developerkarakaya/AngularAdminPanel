
@{
    ViewBag.Title = "Unvan";
    Layout = "~/Views/Shared_/_AdminLayout.cshtml";
}
<script src="http://ajax.googleapis.com/ajax/libs/angularjs/1.4.8/angular.min.js"></script>
<script src="//angular-ui.github.io/bootstrap/ui-bootstrap-tpls-2.0.1.js"></script>
<script src="~/js/personelunvanekleme.js?v=@DateTime.Now.ToString("ddMMyyyyHHmmss")"></script>

<style>
    .item {
        margin-bottom: 20px;
    }
</style>
<h2>Peronel Ünvan Ekleme/Listeleme Sayfası</h2>
<br />
<div class="panel panel-white" ng-controller="personelUnvanEklemeController" ng-app="sirketAdmin">
    <div class="panel-heading clearfix">
    </div>
    <div class="panel-body">
        <div role="tabpanel">
            <div class="tab-content">
                <form class="formPost" id="form">
                    <div class="item">
                        <input type="hidden" name="unvan_no" id="unvan_no" value="{{no}}" />
                        <label>Personel Ünvan</label>
                        <input type="text" placeholder="Personel Ünvan Giriniz.." name="unvan_ad" id="unvan_ad" value="{{unvan}}" class="form-control" />
                    </div>
                    <div class="item">
                        <input type="submit" class="btn btn-success" style="float:right" ng-click="personelUnvanEkle(unvan_no)" value="Kaydet" />
                    </div>
                </form>

            </div>
            <br />
            <hr />

            <br />
            <br />
            <div class="panel panel-white">
                <div class="panel-body">

                    <table class="table table-hover">
                        <thead>
                            <tr>
                                <th>Sıra</th>
                                <th>Ünvan</th>
                                <th>
                                    İşlemler
                                </th>
                            </tr>
                        </thead>
                        <tbody>
                            <tr ng-repeat="item in unvanliste">
                                <th>{{$index + 1}}</th>
                                <td>{{item.unvan_ad}}</td>
                                <td>
                                    <input type="button" value="Sil" class="btn btn-danger" ng-click="unvansil(item.unvan_no)" />
                                    <input type="button" value="Düzenle" class="btn btn-success" ng-click="unvanduzenle(item.unvan_no)" />
                                </td>
                            </tr>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
